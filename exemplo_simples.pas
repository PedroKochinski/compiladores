program simples (input, output);
var x, y: integer;

procedure imprime;
begin
    write(x)
end;

procedure menos (a, b: integer);
var i: integer;
begin
    i := 0;
   
    a := a - b
end;

begin
    read(x);
    read(y);
    menos(x,y);
    imprime

end.