program simples (input, output);
var x,y: integer;

function plus (a,b: integer): integer;
var res: integer;
begin
    a := a + 10;
    b := b + 1;
    plus := a + b
end;

function minus (c,d: integer; e: real): real;
begin
    c := plus(4, 10);
    minus := y
end;